type ThemeType = {
    firstColor: string,
    secondColor: string,
    accentColor: string,
    fontColor: string,
    backgroundColor?: string,
    backgroundImage?: string
}

export default ThemeType;